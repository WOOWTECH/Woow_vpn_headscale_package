# Public URL that Tailscale clients will dial.
# For local testing keep the default; for external access set your
# ngrok URL / domain (must be reachable by clients, HTTPS preferred).
SERVER_URL=http://localhost:28080
